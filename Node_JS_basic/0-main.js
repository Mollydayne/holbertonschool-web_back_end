// Define a function named displayMessage that takes a string as argument
function displayMessage(message) {
  // Print the string to the standard output (console)
  console.log(message);
}

// Export the function so it can be imported in another file
module.exports = displayMessage;
